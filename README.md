# Callgraph_tool
Offline multi-language call graph analyzer for Python, C, C++, MATLAB, and Visual Studio solutions. It scans source code, detects functions and relationships, and generates an interactive self-contained HTML graph with search, filtering, function inspection, and script-level visualization.
